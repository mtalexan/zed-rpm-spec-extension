name: Release rpm-spec-ls binaries

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact_name: rpm-spec-ls-linux-x86_64

          - target: x86_64-apple-darwin
            os: macos-latest
            artifact_name: rpm-spec-ls-macos-x86_64

          - target: aarch64-apple-darwin
            os: macos-latest
            artifact_name: rpm-spec-ls-macos-aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build rpm-spec-ls
        run: |
          cargo build --release --manifest-path rpm-spec-ls/Cargo.toml --target ${{ matrix.target }}

      - name: Rename binary
        shell: bash
        run: |
          mv rpm-spec-ls/target/${{ matrix.target }}/release/rpm-spec-ls \
             ${{ matrix.artifact_name }}

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact_name }}
